# Observability 

## Structured logging for observability

* Logging the caller (e.g. file and line number, package and function) is useful for classifying the log messages during analysis. 

* Can we also give the logger a name? Is that useful?